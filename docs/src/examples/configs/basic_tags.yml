# Minimal QuantumGraph configuration tags loaded from a YAML file.
width: !sweep [16, 32]
epochs: !range [1, 3, 1]
learning_rate: !random_uniform [0.0001, 0.01]
copied_width: !reference width
activation_constructor: !pyobject-equivalent Docs.IdentityActivation
