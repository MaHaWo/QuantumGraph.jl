# File-backed search-space tags. The notebook maps these file-loaded values
# into the nested Julia config consumed by build_search_space.
layers: !sweep [1, 2, 3]
activation: !sweep [relu, tanh]
epochs: !range [1, 3, 1]
learning_rate: !random_uniform [0.0001, 0.01]
